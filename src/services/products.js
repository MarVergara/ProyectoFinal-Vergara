const getAll = () => {};

const add = (newFlower) => {};

const update = (flower) => {};

const getById = (id) => {};

const remove = (id) => {};

export const products = { getAll, getById, add, update, remove };

min 12.22